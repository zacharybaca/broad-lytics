{
  "name": "broad-lytics",
  "version": "1.0.0",
  "description": "BSS/OSS subscription and billing operations SaaS",
  "main": "server/server.js",
  "scripts": {
    "install-all": "npm install && npm install --prefix client && npm install --prefix server",
    "clean-install": "rimraf node_modules client/node_modules server/node_modules **/package-lock.json && npm run install-all",
    "server": "nodemon --prefix server server.js",
    "client": "npm run dev --prefix client",
    "dev": "concurrently \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "format": "prettier --write \"{client/src,server}/**/*.{js,jsx,json,css,md}\"",
    "start": "node server/server.js"
  },
  "keywords": [
    "mern",
    "react",
    "vite",
    "starter"
  ],
  "author": "Zachary Baca",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zacharybaca/broad-lytics.git"
  },
  "bugs": {
    "url": "https://github.com/zacharybaca/broad-lytics/issues"
  },
  "homepage": "https://github.com/zacharybaca/broad-lytics#readme",
  "dependencies": {
    "prettier": "^3.8.3",
    "rimraf": "^6.1.3"
  },
  "engines": {
    "node": ">=20"
  }
}
